@AGENTS.md
## 必須ルール（常時適用）

1. 確認・質問禁止。迷ったら仕様通りに実装して進める
2. 読むファイルは実装対象のみ。無関係なファイルは読まない
3. 1アクションで1ファイルを完成させる。途中で止まらない
4. 完了報告は1行のみ。説明・解説・補足は不要
5. エラーは自己解決する。報告は修正完了後のみ
6. ユーザーへの返答は常に最短・最小限にする
7. ファイル全体を読まない。必要な箇所のみgrep/sedで確認する
8. 実装前にファイル構造を確認しない。指示されたファイルのみ触る
9. 既存コードの引用・表示禁止。変更箇所のみ示す
10. 同じエラーが2回続いたら実装を止めてエラー文のみ報告する
11. 推測で複数ファイルを変更しない。1ファイル変更→確認→次へ
12. Firestore読み書きはすでに定義済みの関数を使う。新規作成しない
13. 静的エクスポート制約（next export）に反する実装は提案しない